# D2 轮播效果修正：剪映「右移」转场（发给 Codex）

> **复制以下内容发给 Codex**

---

## 需求：D2 双图卡片的轮播切换效果改为「右移转场」

### 参考效果
剪映（CapCut）转场中的 **「右移」** 效果：
- **下一组图片从右侧滑入**
- **当前图片同时向左退出**
- 新图从右到左"覆盖"式推进，不是淡入也不是缩放
- 整个过程像一张图片把另一张"推走"

### 技术实现要求

#### 核心原理：两张图同时在同一个容器内水平移动

```
容器 overflow: hidden（裁切掉超出圆角的部分）

状态 A（切换前）：
  ┌─────────────┐
  │  当前图1  │ 当前图2  │     ← 完全可见，translateX(0)
  └─────────────┘

切换过程中（动画进行中）：
  ┌─────────────┐
  │← 当前组（向左滑出）│ 下一组（从右滑入）→│   ← 当前组 translateX(-100%)
  └─────────────┘                              下一组 translateX(0) 从 translateX(100%) 过来

状态 B（切换后）：
  ┌─────────────┐
  │  下一图1  │ 下一图2  │     ← 完全可见，translateX(0)
  └─────────────┘
```

#### 必须严格遵守的 CSS 动画参数

```css
/* 容器 */
.dual-photo-carousel {
  overflow: hidden;          /* 关键！裁切滑动中的溢出部分 */
  border-radius: 10px;
  position: relative;
}

/* 滑动轨道（所有图片组的父容器） */
.dual-photo-track {
  display: flex;
  transition: transform 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  /* ↑ 关键值：0.6秒 + 先加速后减速的物理缓动 = 丝滑不卡顿 */
  will-change: transform;    /* GPU 加速，防止掉帧 */
}

/* 每一组（包含2张并排图片） */
.dual-photo-group {
  flex: 0 0 100%;            /* 每组占满容器宽度 */
  display: flex;
  gap: 6px;                  /* 两张图之间的间距不变 */
  min-width: 0;              /* 防止 flex 子项溢出 */
}
```

#### 动画规格（丝滑不卡顿的关键）

| 参数 | 值 | 说明 |
|------|---|------|
| 过渡属性 | `transform` | 只动 transform，不动 left/top/margin（触发 GPU 合成层，不触发重排重绘） |
| 过渡时长 | **600ms** | 不能少于 500ms（太急），不能超过 800ms（太慢） |
| 缓动函数 | `cubic-bezier(0.25, 0.46, 0.45, 0.94)` | **必须用这个或类似的 ease-out-quart**——先快后慢，模拟真实物体滑动的惯性衰减 |
| 自动间隔 | **2000ms**（每 2 秒切一次） | 切换完成后停 2 秒，再切下一组 |

#### JS 逻辑要点

```javascript
// 核心思路：用一个 track 容器，通过改变 transform: translateX(-N * 100%) 来切换
// 不是分别操作每一张图的显示/隐藏！

let currentIndex = 0;
const totalGroups = Math.ceil(photos.length / 2);  // 每2张一组

function nextSlide() {
  currentIndex = (currentIndex + 1) % totalGroups;
  track.style.transform = `translateX(-${currentIndex * 100}%)`;
}

// 每 2 秒执行一次
setInterval(nextSlide, 2000);

// 用户触摸时暂停，松开后恢复
container.addEventListener('touchstart', () => clearInterval(timer));
container.addEventListener('touchend', () => { timer = setInterval(nextSlide, 2000); });
```

#### 绝对禁止 ❌（这些都会导致廉价/卡顿）

- ❌ 用 `opacity` 做渐隐 → 那是淡入淡出，不是右移
- ❌ 用 `display: none / visibility: hidden` → 瞬间跳变，无过渡
- ❌ 每张图独立做动画 → 会导致不同步、看起来乱
- ❌ 用 `left / top / margin` 做位移 → 触发浏览器重排，**会卡顿**
- ❌ 没有 `will-change: transform` → 移动端容易掉帧
- ❌ 没有 `overflow: hidden` → 图片滑出卡片圆角外
- ❌ 用 `linear` 匀速 → 机械感，没有物理惯性
- ❌ 过渡时间 < 400ms → 太快，看不清滑动过程

#### 丝滑不卡顿的检查清单（让 Codex 自检）

- [ ] 只修改 `transform`，不碰布局属性
- [ ] 使用 `cubic-bezier` 缓动（非 linear）
- [ ] 开启 `will-change: transform`
- [ ] 容器有 `overflow: hidden`
- [ ] 图片已预加载（或至少预读下一组）
- [ ] 手机真机测试不丢帧（60fps）
- [ ] 触摸暂停 / 页面不可见暂停

---

### 一句话总结

> D2 轮播改成**剪映「右移」转场效果**：新图组从右侧 `translateX` 滑入覆盖旧图，600ms + `cubic-bezier(0.25, 0.46, 0.45, 0.94)` 物理惯性缓动 + `will-change` GPU 加速 + `overflow-hidden` 裁切。只动 transform 不动布局属性。每 2 秒自动切一次。
