# 给 ZCode 的开发启动指令（复制本文件 + 《开发提示词与设计建议.md》+ 《图片素材/》文件夹一并发给 ZCode）

你好 ZCode，请帮我开发一个**手机端优先的个人展示网站**（宠物医生 · 治愈系 Kawaii 风格）。请严格按我下面给的资料执行，**不要自由发挥偏离规范**。

---

## 一、你要用的资料（请先全部读取，不要跳过）

1. **《开发提示词与设计建议.md》** —— 本项目的**权威设计规范**，包含：视觉风格系统、4 个页面的完整结构与交互代码、素材资产映射表、抠图铁律、背景纹理系统、风格红线、字体降级方案。**开发时以它为准。**
2. **《图片素材/》文件夹** —— 内含 18 张本人照片(JPG) + 16 张网站素材图(PNG)。每张具体怎么用，查文档 §6 映射表，不要自己瞎猜。

---

## 二、技术栈

- **React + Vite**（构建产物为 `dist/`，纯静态前端，无后端）
- **移动端优先**（目标 iPhone 375–430px）；桌面端做居中适配即可，不要求全功能铺开

---

## 三、⚠️ 必须严格遵守的硬性规则（违反任意一条 = 返工）

1. **素材图绝不整张使用**：网站素材图里一张常含多个主体，必须先 **AI 抠图裁剪出单个主体**（沿轮廓抠成透明底 PNG，存入 `src/assets/images/cutout/` 目录）才能用。详见文档 §6 三条铁律。抠图交 AI，哪款抠得干净高效用哪款。
2. **角色定位**：① 网站素材图中的"女生素材(Leila Q版)"与两只小猫是**平等的陪伴装饰元素，不抢镜、不占更大面积**；② 两只小猫是全站唯一动物吉祥物；③ **本人照片(JPG)才是真正的展示重心**（About Me / 图库 / 轮播）。三者都出现、各司其职。
3. **背景纹理必须纯 CSS 实现**：星芒放射纹 + 粉色格纹，按文档 §3.5.4 **混搭分配**（全站底层淡格纹 + Hero/Loading/收尾用星芒透出 + 收尾区星芒渐变到格纹）。**禁止纯色奶油底**。前景卡片用半透明毛玻璃（`rgba(255,255,255,.72)` + `backdrop-filter: blur(4px)`）。
4. **中文字体跨平台**：使用圆体 Web 字体降级链（ZCOOL KuaiLe 等，见 §10.2），**禁止中文塌成系统默认黑体**。
5. **风格红线**（见 §10.1）：尖角直角 / 冷硬直线 / 模板感(Bootstrap 那类) / 出现猫以外的动物 / 高饱和荧光色 / 机械居中死板 / 纯色无纹理背景 —— 出现任意一条都打回重做。
6. **省 Token**：能用 CSS/SVG 画的装饰（图钉 / 夹子 / 胶带 / 涂鸦 / 脚印 / 按钮图标）一律代码实现，不要生成图片；猫咪 / 人物优先用素材图抠图后的产物，不要重新生成新图。

---

## 四、本次开发范围（只开发，不部署）

- 完整实现 4 个页面：
  1. **全屏 Hero**（多色块拼贴 + 居中主视觉 + 大面积手绘涂鸦）
  2. **个人展示 About Me**（紧凑不对称拼贴 + 长按换图功能）
  3. **图库照片墙**（真实照片墙 + 深色墙板 + 拍立得/抠图两种 + 15 张上限 + 随机位置 + 点击放大删除）
  4. **收尾 3D 视差轮播**（Coverflow 风格）
- 附加功能：🐾 手绘风页面进度指示器 + Loading 加载页
- 所有交互、动效、响应式按文档实现
- **本次不需要部署到公网** —— 开发完在本地 `npm run dev` 自测即可，等我审核

---

## 五、交付要求

1. 先给出你的**开发计划与文件结构**，确认无误再动手写代码
2. 每完成一个页面告诉我进度
3. 全部完成后，告诉我如何在本地预览（`npm run dev` 或 `npm run build` 后打开 `dist/`）
4. **不要自动部署上线**，等我（用户）审核满意后，再单独处理公网部署

请开始。
