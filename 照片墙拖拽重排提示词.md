# 照片墙拖拽重排功能（发给 Codex）

> **复制以下内容发给 Codex**

---

## 需求：给 PhotoWall（照片墙）添加「长按拖拽移动位置」功能

### 用户场景
照片墙上的图片是随机散布的，有时多张图堆叠在一起（如截图红圈区域），互相遮挡。用户希望能通过 **长按某张图 → 拖拽 → 松手放到新位置** 的方式，手动调整照片的位置，避免堆叠遮挡。

### 参考效果
类似 iOS 桌面图标拖拽 / Pinterest 图钉板拖拽的感觉：
1. **长按触发**（和已有的"长按换图"功能区分开：长按短暂触发换图，长按稍久触发拖拽）
2. **被拖的图浮起**（放大 + 加深阴影 + 半透明/轻微发光）
3. **跟随手指移动**
4. **松手放置**（落到新坐标 + 轻弹动画归位）
5. **保存新位置到 localStorage**

---

## 技术实现要求

### 1. 交互逻辑：长按 vs 点击 vs 拖拽 三者分离

```
touchstart → 记录起始时间 + 坐标
  ├─ touchend 在 300ms 内且位移 < 10px → 【点击】→ 打开 Lightbox 大图预览
  ├─ touchend 在 300ms~800ms 且位移 < 10px → 【长按】→ 触发换图功能（已有）
  └─ touchmove 位移 > 10px（在任意时刻）→ 【拖拽】进入拖拽模式，取消换图计时器
       └─ touchend → 【放置】保存新位置
```

| 手势 | 判定条件 | 触发动作 |
|------|---------|---------|
| **点击（tap）** | `touchstart → touchend < 300ms` 且手指没怎么动 | 打开 Lightbox 预览 |
| **长按（hold）** | `touchstart → 保持 500ms+` 且手指没动 | 触发换图（已有功能） |
| **拖拽（drag）** | `touchstart → touchmove > 10px`（不管按了多久） | 进入拖拽模式 |

### 2. 拖拽过程中的视觉效果

```css
/* 正常状态 */
.photo-item {
  position: absolute;          /* 已有：绝对定位散布 */
  transition: left 0.3s, top 0.3s;  /* 放置时的回弹动画 */
  cursor: grab;
  z-index: var(--base-z, 1);
}

/* 拖拽中状态（JS 动态加 .dragging class）*/
.photo-item.dragging {
  cursor: grabbing;
  transform: scale(1.08);        /* 放大 8%，表示"拿起来了" */
  box-shadow: 0 16px 40px rgba(0,0,0,0.25);  /* 深阴影 = 浮起感 */
  z-index: 1000;                 /* 拖拽时最高层 */
  transition: none;              /* 拖拽中取消过渡，跟随手指无延迟 */
  opacity: 0.9;                  /* 轻微半透明，能看到下面的东西 */
  filter: drop-shadow(0 8px 16px rgba(0,0,0,0.2)); /* 外发光效果 */
}
```

### 3. 拖拽核心 JS 逻辑

```javascript
// ===== 核心变量 =====
let isDragging = false;
let draggedPhoto = null;
let startX, startY, initialLeft, initialTop;
let longPressTimer = null;

// ===== touchstart =====
photo.addEventListener('touchstart', (e) => {
  // 记录初始状态
  const touch = e.touches[0];
  startX = touch.clientX;
  startY = touch.clientY;
  initialLeft = parseFloat(photo.style.left);
  initialTop = parseFloat(photo.style.top);

  // 启动长按计时器（用于换图功能）
  longPressTimer = setTimeout(() => {
    if (!isDragging) triggerLongPressSwap(photo);  // 已有的换图功能
  }, 500);

  // 监听后续 move 来判断是否进入拖拽
}, { passive: true });

// ===== touchmove（关键：判断是否从"长按"切换到"拖拽"）=====
photo.addEventListener('touchmove', (e) => {
  const touch = e.touches[0];
  const dx = touch.clientX - startX;
  const dy = touch.clientY - startY;

  // 如果手指移动超过 10px → 进入拖拽模式
  if (Math.abs(dx) > 10 || Math.abs(dy) > 10) {
    if (!isDragging) {
      // 首次超过阈值：进入拖拽模式
      isDragging = true;
      draggedPhoto = photo;
      clearTimeout(longPressTimer);  // 取消换图计时器

      // 加上 dragging 视觉样式
      photo.classList.add('dragging');

      // 取消该元素的 transition（让跟随无延迟）
      photo.style.transition = 'none';
    }

    // 实时更新位置（跟随手指）
    const newLeft = initialLeft + dx;
    const newTop = initialTop + dy;

    // 边界限制：不让照片拖出墙板范围
    const wallRect = wallContainer.getBoundingClientRect();
    const photoRect = photo.getBoundingClientRect();
    const constrainedLeft = Math.max(0, Math.min(newLeft, wallRect.width - photoRect.width));
    const constrainedTop = Math.max(0, Math.min(newTop, wallRect.height - photoRect.height));

    photo.style.left = constrainedLeft + 'px';
    photo.style.top = constrainedTop + 'px';
  }
}, { passive: true });

// ===== touchend（放置）=====
photo.addEventListener('touchend', () => {
  clearTimeout(longPressTimer);

  if (isDragging && draggedPhoto) {
    // 退出拖拽模式
    isDragging = false;

    // 移除 dragging 样式
    draggedPhoto.classList.remove('dragging');

    // 恢复 transition（让"放置"有弹性动画）
    draggedPhoto.style.transition = 'left 0.3s cubic-bezier(0.34, 1.56, 0.64, 1), top 0.3s cubic-bezier(0.34, 1.56, 0.64, 1)';
    // ↑ 注意这里用 ease-out-back 缓动（cubic-bezier 弹性），让放下时有轻微"弹一下"的物理感

    // 保存新位置到 localStorage
    savePhotoPosition(draggedPhoto.id, draggedPhoto.style.left, draggedPhoto.style.top);

    draggedPhoto = null;
  }
});
```

### 4. 数据持久化

```javascript
function savePhotoPosition(photoId, newLeft, newTop) {
  // 从 localStorage 读取当前照片墙数据
  const wallData = JSON.parse(localStorage.getItem('photoWallData') || '{}');

  // 找到对应照片并更新坐标
  const photo = wallData.photos.find(p => p.id === photoId);
  if (photo) {
    photo.left = newLeft;
    photo.top = newTop;
    photo.lastMoved = Date.now();  // 记录最后移动时间（可选）

    // 写回 localStorage
    localStorage.setItem('photoWallData', JSON.stringify(wallData));
  }
}
```

### 5. 边界约束（重要！防止拖丢）

```javascript
// 每次更新位置时做边界 clamp：
function constrainPosition(left, top, photoEl, wallEl) {
  const wallRect = wallEl.getBoundingClientRect();
  const photoRect = photoEl.getBoundingClientRect();

  return {
    left: Math.max(0, Math.min(left, wallRect.width - photoRect.width)),
    top: Math.max(0, Math.min(top, wallRect.height - photoRect.height))
  };
}
```

- 照片不能拖出墙板圆角区域外
- 至少保留 4px 内边距（不要贴边）
- 如果墙板尺寸变化（屏幕旋转），需要重新 clamp

### 6. PC 端兼容（鼠标操作）

```javascript
// 除了 touch 事件，还要支持 mouse 事件（PC 浏览器调试用）
photo.addEventListener('mousedown', startDrag);   // 对应 touchstart
document.addEventListener('mousemove', onDrag);     // 对应 touchmove（挂在 document 上）
document.addEventListener('mouseup', endDrag);      // 对应 touchend（挂在 document 上）
```

> ⚠️ mouse 事件要挂在 `document` 上而不是 photo 上，否则鼠标移出照片范围就丢失事件了。

### 7. 性能优化

- [ ] 拖拽过程中**暂停碰撞检测**（AABB 检测在拖拽中不需要跑，等放置后再检测一次即可）
- [ ] 使用 `transform: translate()` 做拖拽跟随（比直接改 left/top 更高性能），**放置后才写回 left/top**
- [ ] `touchmove` 和 `mousemove` 用 `{ passive: true }`（如果不需要 preventDefault），避免滚动阻塞
- [ ] 拖拽时给墙板容器加 `user-select: none` 和 `-webkit-user-drag: none`，防止手机端误触选中文本/滚动页面

### 8. 可选加分项（有余力再做）

- **拖拽时的"吸附"效果**：当照片靠近另一张图边缘 < 20px 时，显示淡蓝色对齐辅助线（类似 Figma / Canva 的智能引导线）
- **撤销功能**：放置后 3 秒内出现「撤销」按钮（toast 形式），点一下回到拖拽前的位置
- **自动整理按钮**：工具栏加一个"一键打散"按钮，重新随机分布所有照片（解决严重堆叠问题）

---

## 绝对禁止 ❌

- ❌ 拖拽时照片消失或闪烁
- ❌ 拖拽导致页面整体滚动（必须 `preventDefault` 或 CSS `touch-action: none`）
- ❌ 拖拽后位置没有保存（刷新回到原位）
- ❌ 长按换图和拖拽冲突（同时触发两个功能）
- ❌ 照片可以拖到墙板外面看不见的地方
- ❌ PC 端完全不能用（至少要能用鼠标调试）

---

## 一句话总结

> 给照片墙加**长按拖拽重排**：touchmove 位移超 10px 自动进入拖拽模式（取消换图计时器），拖拽中 scale(1.08)+深阴影+z-index 提升浮起，跟随手指实时移动并做边界 clamp，松手后 elastic-back 缓动归位 + 写入 localStorage 新坐标。同时兼容 mousedown/mousemove/mouseup 做 PC 调试。
