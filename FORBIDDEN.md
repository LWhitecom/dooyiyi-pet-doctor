# 🚫 FORBIDDEN.md — 宠物医生小站 · 禁止清单

> 本清单为事中约束。每轮交付必须逐条回报状态：✅已验证 / ❌失败 / 未验证。
> 来源：开发文档 §10.1 风格红线（8 条）+ 启动提示词硬性规则（6 条）+ 省 Token 铁律。

---

## R1 · 素材图绝不整张使用（启动规则 1 / 文档§6 铁律）
**禁止**：把 `网站素材图N.png` 整张直接 `<img src>` 贴到网页上当背景/主图/装饰。
**正确**：素材图是「多元素素材板」，必须先 AI 抠图裁单主体存入 `src/assets/images/cutout/` 才能用。
**本轮状态**：本轮根本不用网站素材图，只用本人照片(无需抠) + 纯CSS/SVG 占位。抠图另开一轮。

## R2 · 角色定位（启动规则 2）
**禁止**：① 女生素材+猫抢镜占更大面积 ② 出现猫以外的任何动物 ③ 把素材女生当展示重心。
**正确**：女生素材 + 两猫 = 平等陪伴装饰（不抢镜）；本人照片 = 真展示重心。
**本轮状态**：本轮用纯CSS/SVG 简化猫占位（非整图），本人照片作主展示。符合。

## R3 · 背景纹理纯 CSS（启动规则 3 / 风格红线第 8 条）
**禁止**：① 纯色奶油底（`background:#FFF8E7` 无纹理）② 用图片做背景纹理。
**正确**：星芒放射纹 + 粉色格纹，CSS `conic/radial/repeating-linear-gradient` 混搭（全站底层淡格纹 + Hero/Loading/收尾星芒 + 收尾渐变过渡）。
**检查点**：`body` 必须有 `background-image`，不能只 `background-color`。

## R4 · 卡片毛玻璃（启动规则 3 / 文档§3.5.4）
**禁止**：纹理背景上用纯白不透明卡片（突兀像贴纸）。
**正确**：`background:rgba(255,255,255,.72); backdrop-filter:blur(4px); border:2px solid rgba(255,255,255,.9)`。

## R5 · 中文圆体 Web 字体降级链（启动规则 4 / 风格红线第 7 条）
**禁止**：中文塌成系统默认黑体（安卓/Windows 渲染失败）。
**正确**：`--font-cn-round` 降级链，Google Fonts 引 ZCOOL KuaiLe。
**检查点**：`index.html` 有 Google Fonts link；中文元素 `font-family: var(--font-cn-round)`。

## R6 · 风格红线（启动规则 5 / 文档§10.1，出现任意一条打回）
- ❌ 尖角直角（圆角必须 ≥16px）
- ❌ 冷硬线条（直线分隔/硬阴影/几何边框）
- ❌ 模板感（Bootstrap/Ant Design/Material 外观）
- ❌ 非猫动物（兔子/熊/鸭子/小猪/狐狸）
- ❌ 高饱和/荧光色（纯红/纯蓝/荧光粉/霓虹绿）
- ❌ 居中死板（机械居中/等距网格，应散落错位拼贴）
- ❌ 中文塌黑体（见 R5）
- ❌ 纯色无纹理背景（见 R3）

## R7 · 省 Token（启动规则 6 / 文档§省 Token 八条）
**禁止**：图钉/夹子/胶带/涂鸦/脚印/按钮图标去生成图片。
**正确**：CSS/SVG 代码实现；猫咪/人物优先抠图产物，不重新生图。

## R8 · 范围与部署（启动提示词四/五）
**禁止**：① 自动部署上线（必须用户明确说"满意，可以部署"）② 顺手优化/重构无关代码 ③ 只改指定范围外的东西。
**正确**：本次只开发 + 本地预览自测，等审核。

---

## 每轮回报模板
```
【禁止清单回报】
- R1 ✅/❌（证据）
- R2 ✅/❌
- R3 ✅/❌
- R4 ✅/❌
- R5 ✅/❌
- R6 ✅/❌（逐条子项）
- R7 ✅/❌
- R8 ✅/❌
```
